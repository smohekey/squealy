[package]
name = "squealy-postgresql"
version = "0.1.0"
edition = "2024"
description = "PostgreSQL backend for squealy"
license = "MIT"

[dependencies]
bytes = "1"
futures-core = "0.3"
squealy = { path = "../squealy" }
tokio-postgres = "0.7"

[dev-dependencies]
futures-util = "0.3"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
