{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":3,"timestamp":1784344468,"nonce":"4E1pK3ermPMEpU6jFqqOBKi1/fE=","message":"Phase 3 (model the unmodeled objects) — PR 1 of the object series: MERGED PR #111 (squash 1839dc6).\n\nPhases 0-2 are confirmed DONE and fully wired across PG/MySQL/SQLite (the on-disk\nsquealy-phase2-plan.md doc was stale, stopped at PR 2.0b; the code has CTEs + all three\nper-backend view-body introspections). No DML/statement IR exists (lower_query bottoms out\nat SELECT). Phase 3 is now sequenced as an ordered set of mostly-independent PRs in\ndifficulty/momentum order (see ~/.claude/plans/squealy-phase3-plan.md): 1. MySQL NOT ENFORCED\n(this PR) -\u003e 2. PG enums -\u003e 3. sequences -\u003e 4. domains -\u003e 5. exclusion constraints -\u003e\n6. materialized views -\u003e [DML statement IR foundation] -\u003e 7. triggers.\n\nPR 1 (this one): activated MySQL CHECK enforcement, which was plumbed end-to-end (IR / KDL /\ncapability) but rejected at render and dropped on introspect. MySQL now renders/introspects/\nround-trips CHECK (...) NOT ENFORCED, mirroring PG's existing validation pattern; cap\ncheck_enforcement=true (checks only). The capability flip was trivial, but activating a dormant\nfield drew 3 real codex rounds, each reproduced + pinned: (R1) an explicit Some(Enforced)\nchurned, KDL-reachable, and PG had the SAME latent gap with Some(Validated) -\u003e fixed in shared\ncanonicalize_model (folds the explicit Validated/Enforced defaults to None for checks + FKs,\nmirroring normalize_no_action; also closes the parallel PG validation churn); (R2, P1 data loss)\nan enforcement toggle rendered DROP+ADD, which on MySQL's auto-committed DDL loses the constraint\nwhen enabling enforcement on violating rows -\u003e atomic ALTER CHECK for enforcement-only changes;\n(R3) that fast path skipped write_check's validation reject (incremental plan path skips\nvalidate_capabilities) -\u003e gated on after.validation.is_none(). Verified: 969 tests, MySQL live\n13/13, PG 4/4, model 7/7; CI test + MSRV green; codex round 4 clean.\n\nLESSON (recorded): activating a dormant capability ripples into places the surrounding machinery\ndid not anticipate (canonical default fold; non-atomic ALTER; a new render path needing the\ncapability guard). The plan's framing of PR 1 as the \"warm-up that proves the object-add loop\"\nheld exactly.\n\nNEXT = PR 2 (PG enums): retire the Raw fallthrough (postgresql/introspect.rs sql_type catch-all)\nso a PG enum column resolves to a modeled CREATE TYPE ... AS ENUM instead of referencing a type\nsquealy never emits. First substantial object — a full run of the VIEW threading template.","files":null}]}