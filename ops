{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":3,"timestamp":1783323813,"nonce":"1z5gEY69X1pSmA8dVk4X0yRNrdo=","message":"Phase 0 (Foundation) — DONE, merged in PR #83 (squash 40d869d).\n\n- New `squealy-parse` crate pinning `sqlparser = \"=0.62.0\"` as the shared parse front-end. Placed\n  *below* the backends in the crate graph (deps only on core `squealy`) so a backend's introspection can\n  call it to reconstruct bodies, while the parser's transitive weight stays out of the query-builder core.\n  `SqlDialect` → sqlparser-dialect map; `parse_sql`/`parse_expr` wrappers (`parse_expr` requires EOF so\n  trailing tokens can't silently truncate a read).\n- `Reader` seam mirroring the renderer (read_create_view / read_check_expression /\n  read_generated_expression / read_index_expression); routes through `lower::lower_expr` / `lower_query`,\n  which return `NotYetLowered` in Phase 0.\n- `normalize` module = the design catalogue of render idioms a reverse parser must invert (full\n  parenthesization, CAST-result pins, float-cast division, `||`↔CONCAT, substr↔SUBSTRING, empty-`IN ()`\n  sentinel, FLOOR(EXTRACT(SECOND …)), …), each with its semantics-safety note. `lower` = AST→model stubs.\n- Round-trip identity harness (`squealy-model/tests/roundtrip.rs`) standing up the epic's\n  `render(parse(render(m))) == render(m)` spine over a curated corpus (arithmetic / aggregates /\n  predicates / LIKE-BETWEEN-IN / CASE-COALESCE-NULLIF / string fns / temporal; plus check / generated /\n  partial-index tables). Asserts **sqlparser parses 100% of squealy's rendered output** — 28 render+parse\n  checks across 10 cases × 3 backends, with exact coverage pinned and only the two documented backend\n  limits whitelisted (SQLite generated columns, MySQL partial-index predicates). This validates the\n  sqlparser choice and de-risks the lowering phases.\n\nReview: local codex 2 rounds; GitHub @codex 4 rounds → clean. Fixes landed: trailing-token EOF check;\nwhitelist + exact-coverage assertion for render skips; view read routed through the lowering seam.\n\n- [x] Phase 0 - Foundation.\n\nNEXT = Phase 1 (Expression normalization): implement `lower_expr` + the `normalize` folds to structure\nCHECK / generated-column / index expressions into `ExprNode` (no view type inference needed).\nIntrospection stores the normalized form; the diff compares that → kills the perpetual `Alter*` churn.\nPG → MySQL → SQLite, one PR per backend. Relates to 108fdfc.","files":null}]}