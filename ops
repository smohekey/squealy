{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":1,"timestamp":1782804675,"nonce":"UIuBOpiSsmZUHc7nnhd5k7xMbzc=","title":"Even out cross-backend render-test coverage for subqueries/CTEs/set-ops","message":"Compositional query support (e786bc1) is fully implemented and the renderer is shared (render.rs drives PG + MySQL via Dialect), but the render-test coverage is uneven across backends. Belt-and-suspenders: mirror a few existing test-backend render assertions onto PG and MySQL so dialect-specific rendering (placeholders, identifier quoting) is pinned for each.\n\nGaps noted during the e786bc1 audit (render tests, no DB):\n- Scalar subqueries (projection + predicate): only on the test backend; add PG + MySQL render tests.\n- IN (subquery) / EXISTS: PG has render tests; add MySQL.\n- Set ops: INTERSECT not render-tested on MySQL; EXCEPT not render-tested on PG; add the missing operators per backend.\n- Nested CTEs: not render-tested on MySQL.\n\nLow priority (tests only, no behavior change). These exercise the shared renderer through each Dialect, so they mostly pin placeholder/quoting differences rather than new logic.","files":null}]}