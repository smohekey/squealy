{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":3,"timestamp":1783342302,"nonce":"MUMgRhlpKl/g8yVD0bEbhcm63CM=","message":"Phase 1 — PR 1 (lowering foundation) MERGED as #84 (squash de0177f).\n\nDesign decision (maintainer): migrate the constraint model fields String -\u003e ExprNode (structural,\ncross-dialect). Since a field-type flip is atomic, Phase 1 is split: PR 1 = the reverse-parser lowering\n+ IR widening (no field flip); PR 2 = flip the fields.\n\nShipped in PR 1:\n- `ExprNode::BareColumn { column }` (unqualified column refs; dedicated variant preserves the\n  \"view columns always qualified\" invariant).\n- `squealy::render_scalar_expr` (public core renderer for scalar/constraint ExprNode).\n- `squealy_parse::lower::lower_expr` inverting the renderer's scalar idioms: parenthesization,\n  float-cast division, `||`/`CONCAT`, `SUBSTRING`/`substr`, `CHAR_LENGTH`/`length`, `LIKE`/`ILIKE`,\n  columns/literals/arith/compare/logical/IN/BETWEEN + closed scalar-fn set. Dialect-gated for semantic\n  safety (bare `/` -\u003e MySQL only; `||` -\u003e pipe dialects; MySQL `LENGTH` != CHAR_LENGTH; float-cast peel\n  matches the dialect's idiom type; regex SUBSTRING rejected). Empty-`IN ()` sentinel recovered.\n- Backend `dialect()` accessors + a render-\u003eparse-\u003elower-\u003erender identity harness across PG/MySQL/SQLite.\n\nReview: local codex 4 rounds (6 findings fixed), GitHub @codex 1 round (2 P2s fixed) -\u003e clean.\n\nDeferred to PR 2 (field flip): (a) canonicalize the `Like.case_insensitive` flag on MySQL/SQLite in the\ndiff (render-lossy there); (b) general cross-dialect CAST inversion; (c) general function node for\narbitrary user-check functions.","files":null}]}