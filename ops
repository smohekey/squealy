{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":6,"timestamp":1782000077,"nonce":"7AgDLboTl0MSflSvWC5psRHnxkY=","target":"f19323f5d0b56db171f54640ffb9c33556813ae7da917fd3de0ad39c7209c6f1","message":"**Summary**\nAdd `right_join` and `full_join` to complete the join matrix (only `join` (inner)\nand `left_join` exist today).\n\n**Motivation**\nCommon join shapes; rounds out the sources surface.\n\n**Plan**\n- `left_join` wraps the NEWLY-joined table as nullable via `Maybe\u003cS\u003e`. The\n  asymmetry:\n  - `right_join`: joined table non-null, but the ACCUMULATED (base) exprs become\n    nullable.\n  - `full_join`: BOTH sides nullable.\n- New machinery: a way to nullable-wrap the accumulated `Exprs` HList (current\n  design only wraps the new table). Build `RightJoinTarget`/`FullJoinTarget`\n  mirroring `JoinTarget`/`LeftJoinTarget` + their `.on(...)`.\n- Source specs: `RightJoinSource`/`FullJoinSource` paralleling\n  `InnerJoinSource`/`LeftJoinSource`; render `RIGHT JOIN` / `FULL JOIN`.\n- Type-level: wrap the joined/base column kinds in `Nullable\u003c…\u003e` on the relevant\n  side(s); decoding flows as `Option\u003cT\u003e` (reuse existing nullable projection).\n\n**Files**\ncrates/squealy/src/query.rs (targets, sources, SourceQuery verbs), render.rs +\nsquealy-test/src/sql.rs (push_right_join / push_full_join on `SelectSink`),\nlib.rs, + tests.\n\n**Backends**\nPlain `RIGHT/FULL JOIN` keywords (MySQL supports both); no `Dialect` divergence.\n\n**Tests**\nRender (alias + nullable wrapping), per-backend, compile-fail if a non-nullable\ndecode is requested for a now-nullable side, ignored round-trip.\n\n**Size**\nSmall for `right_join` (mostly mirror) + medium for accumulated-side nullable\nwrapping; ~one session total.","files":null}]}