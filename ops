{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":3,"timestamp":1783475287,"nonce":"izULeH0Trwu/vHSNQy0heu2NCfg=","message":"Phase 1 carry-forward merged: general `ExprNode::Function` node (PR #90, squash `21bfd14`).\n\nAdds a general, dialect-neutral function-call node so a **column-argument** user/built-in function\n(`jsonb_typeof(data) = 'object'`) in a CHECK / generated-column / index expression lowers\n**structurally** instead of falling to `Raw`, and re-plans to empty (converging the authored and\n`pg_get_constraintdef` deparse forms).\n\n**Scope decision (maintainer, \"Option A\"):** the node is produced only for an **unquoted name with\nfully-structural, literal-free arguments**. A quoted name (`\"MyFunc\"`), a direct literal argument\n(`my_func('x')` — PostgreSQL deparses `'x'::text`), or any un-lowerable argument stays `Raw`.\n`canonical.rs` is **KEPT** (not deleted): local codex proved it also absorbs `%` modulo + general\n`CAST` `Raw` checks, so deleting it would regress those; `canonical_check_expression` keeps its `Raw`\nstring-normalizer fallback.\n\n**Reviews:** local codex 4 rounds → clean; GitHub @codex 2 P2s fixed (a render/read asymmetry —\nliteral args, then raw args — closed by enforcing a \"fully-structural args\" invariant at the KDL\ndeserialize boundary + documenting it on the variant) → clean on `7df3d42`. CI (MSRV + test) green.\n\n**Follow-up to fully retire `canonical.rs`** (file if pursued): implement `ArithmeticOp::Modulo` (`%`)\nend-to-end + general cross-dialect `CAST` inversion (the MySQL `SIGNED`/`UNSIGNED` ambiguity). Once\nthose residual `Raw` shapes lower structurally, `canonical.rs` can be deleted.\n\n**Phase 1 status:** all four constraint-expression fields migrated to structural `ExprNode` (checks\nstructuring win (#90). Next epic step: Phase 2 (view-body reconstruction) per the round-trip plan, or\nthe canonical.rs-retirement follow-up.","files":null}]}