{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":6,"timestamp":1782000077,"nonce":"OHQG14BmHg3C18rEITmAsXpj7qY=","target":"d5d1a2113db95653633f468ec6e463358fec3b76cb4cb82cf832b9bb406ed805","message":"**Summary**\nAdd Common Table Expressions: `WITH name AS (...) SELECT ...`, then\n`WITH RECURSIVE`.\n\n**Scope**\n- v1: non-recursive single/multiple CTEs referenced as FROM/join sources.\n- Deferred: `WITH RECURSIVE` (self-reference + UNION; depends on the set-ops\n  issue).\n\n**Plan**\n- Builder: name a subquery and reference it as a source, e.g.\n  `conn.with::\u003cName\u003e(|sub| ...)...from_cte::\u003cName\u003e()...`. Exact surface TBD in\n  design.\n- Type-level: a CTE introduces a NEW named source whose projection shape derives\n  from the inner query's `Row`; referencing it needs a new `SourceSpec` kind (a\n  named reference, not a table). The `q{depth}_{index}` alias machinery and the\n  `RenderSubquery` infra (from subqueries) are the building blocks.\n- Rendering: emit the `WITH name AS (...)[, ...]` prefix before the main SELECT;\n  CTE bodies render first, with shared param numbering (borrowed `Renderer`).\n- Recursive form: self-reference inside the body + `UNION [ALL]` between base and\n  recursive terms — build on the set-ops work.\n\n**Files**\ncrates/squealy/src/query.rs (CTE source kind, naming, WITH plumbing), render.rs +\nsquealy-test/src/sql.rs, lib.rs, + tests.\n\n**Tests**\nRender (single + multiple CTEs, CTE in FROM and in a join), bind ordering (bodies\nfirst), per-backend, ignored round-trip; recursive CTE as a follow-up.\n\n**Size**\nLarge (~one+ session) — new source kind + WITH rendering; recursive is a further\nincrement.\n\n**Depends on**\nSet operations (issue) for the recursive variant.","files":null}]}