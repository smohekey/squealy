{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":3,"timestamp":1783458593,"nonce":"Ve9ebZW4gP39oE1lxRJAzIMo2K4=","message":"Phase 1 (2b) merged as PR #89 (squash f481ad4): `GeneratedColumnModel.expression` migrated from\n`String` to `Option\u003cExprNode\u003e` — the last Phase-1 constraint-expression field to become structural.\n\nThis completes the Phase-1 field flips: CHECK expressions (#86), index expressions (#87), index\npredicate (#88), and now generated-column expressions (#89) all lower to structural `ExprNode`, so a\nsquealy-published table carrying any of them re-plans to empty instead of churning.\n\n`Option` because `#[column(generated)]` is a bare flag with no expression syntax — a macro-built model\ncarries `None` (renderer rejects it), while a real expression arrives only from KDL or introspection as\n`Some`. Wired end-to-end mirroring the CHECK migration: Reader `read_generated_expression_or_raw`;\nPG/MySQL render via `render_scalar_expr` + reject-on-`None`; PG/MySQL introspection routes the deparse\n(`pg_get_expr` / `GENERATION_EXPRESSION`) through the Reader; KDL stores a structural\n`generated-expr { \u003cexpr\u003e }` child with legacy string-prop back-compat; the new\n`SchemaIntrospect::canonical_generated_expression` seam re-parses `Raw` in the backend dialect (kept\nverbatim if un-lowerable, since the canonical model feeds the rendered `GENERATED ALWAYS AS (…)`) and\n`normalize_expr` runs on both sides. SQLite is unchanged — it still rejects all generated columns.\n\nLocal codex clean first pass; GitHub @codex approved; CI green (MSRV + test). Verify: 811 tests,\ncompile_fail, fmt, clippy.","files":null}]}