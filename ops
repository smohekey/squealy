{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":3,"timestamp":1782962538,"nonce":"7eXVa4phNSQORVxL8wg1BdBYXjM=","message":"PR 2b merged (#70, squash 3432889): the driver-free SQLite query layer — query objects for SELECT/INSERT/UPDATE/DELETE/set-ops/UPDATE…FROM/DELETE…USING with to_sql/collect_params through the shared renderer + SqliteDialect.\n\nCodex review ran 6 rounds; all findings fixed and verified against a live sqlite3. Notable core additions (default-preserving, so PG/MySQL are unchanged):\n- Dialect seams: qualify_schema, set_operand_style (SetOperandStyle::SubquerySelect — SQLite wraps set operands as SELECT * FROM (…), which fixes ordered operands + nested-group precedence), substring_uses_function_call (substr), delete_using_style (SqliteExists → WHERE EXISTS), concat_uses_pipe_operator (||), unary_string_fn_name (length).\n- Type-level rejection of SQLite-invalid SQL (matching the existing row-locked-set-operand style): SupportsIntersectExceptAll (no INTERSECT/EXCEPT ALL), SupportsColumnlessUpsert (no DEFAULT VALUES ON CONFLICT), SupportsDefaultKeyword (no DEFAULT in VALUES/SET), SupportsExtract (no EXTRACT — SQLite uses strftime).\n\nRemaining for this bug:\n- PR 3: execution via tokio-rusqlite (rusqlite::Connection is !Send; run on a dedicated thread) + Executable* impls.\n- PR 4: introspection + schema/metadata/history stores + render_plan table-rebuild (12-step) + views (39f4468).\n\nFiled a related follow-up: derive macros should capture /// doc comments (630bed8).","files":null}]}