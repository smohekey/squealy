{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":3,"timestamp":1783505302,"nonce":"lXN7aQkfbfBR1OQUOm744lx8eUk=","message":"Phase 2, PR 2.0a MERGED (PR #92, squash `9a3fc93`) — first IR widening: derived-table (subquery) FROM/JOIN sources.\n\n`ViewQueryModel` now expresses a derived table as a source. New `SourceItem { Named(SourceRef) | Derived { query: Box\u003cViewQueryModel\u003e, alias } }` in squealy-ir; `ViewQueryModel.from` + `JoinItem.source` flipped `SourceRef` → `SourceItem`. Threaded through: `render_source` (renders `(\u003csubquery\u003e) AS alias`, inner projections aliased), `collect_query_sources` (recurses derived bodies via `collect_source_item`; a derived alias is a local binding, not a dependency), `ModelSink::source_ref` (→ `Named` — the typed builder only references named relations; authoring derived views is Track F, a follow-up), `lower_source` (lowers `TableFactor::Derived` non-LATERAL/unsampled/aliased via recursive `lower_query`, else `NotYetLowered`), and KDL ser/de (`view_source_item_to/from_node`: derived = `alias` prop + nested `query` child, no name arg is the discriminant; `join_on_expr` skips the `query` child so the join `ON` still round-trips).\n\nTests: roundtrip.rs `view/derived-table` case (render→parse→lower→render identity on all 3 dialects + model equality PG/MySQL, SQLite schema-exempt); 3 lower.rs unit tests (derived FROM, derived JOIN, LATERAL/nested-set-op rejection); package.rs KDL round-trip (derived FROM + derived LEFT JOIN). Full verify green; local `codex review --base main` clean; GitHub @codex clean (\"Didn't find any major issues\"); CI green.\n\nNext in the foundation series: PR 2.0b (set-ops in a view body — recursive Select|Set body), then 2.0c (CTEs / recursive CTEs), then per-backend introspection wiring 2.1 PG / 2.2 MySQL / 2.3 SQLite.","files":null}]}