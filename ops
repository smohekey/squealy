{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":3,"timestamp":1783577163,"nonce":"L9oG1alJRQA4LdIZ771NIWkKbFc=","message":"Addendum (corrections to the note above, after the full review loop):\n\n- Review was **local codex (4 rounds) + GitHub @codex (6 rounds)**, all clean. GitHub rounds each surfaced one real SQLite-verified edge case in recursive-CTE rendering: (a) unscopable recursive arms — a per-arm ORDER BY/LIMIT/OFFSET or nested set is rejected (a recursive-CTE grammar forbids parenthesizing an arm); (b) a recursive CTE behind a leading `WITH` prelude renders by recursing through the `With` layer to the recursive `Set`; (c) INTERSECT/EXCEPT recursive bodies rejected (must be UNION).\n\n- **WITH-scoping correction:** `collect_body_sources` drops an unqualified source matching **ANY** CTE in the `WITH` (not just preceding ones). Verified vs real `sqlite3`: a forward CTE reference resolves to the CTE and shadows a same-named external table, and a non-recursive self-reference is in scope (\"circular reference\"); PG/MySQL reject the forward/self shape, so it's SQLite-only where mutual visibility holds. Schema-qualified sources are always kept.\n\n- Deferred pre-existing bug (column-listed body ORDER-BY-on-alias re-renders invalid SQL) is git-bug **e1d0724**.","files":null}]}