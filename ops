{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":1,"timestamp":1781996727,"nonce":"UIeGHJ9emNl7eDqj0gqFxLu/QCI=","title":"Add `SELECT DISTINCT` support to the query builder.","message":"Common read pattern; the only remaining piece of the aggregation/dedup story\n(see the older aggregation-pipeline issue, now mostly delivered — GROUP BY /\nHAVING / aggregates are done). DISTINCT is independent of GROUP BY.\n\n- v1: plain `SELECT DISTINCT` over the whole projection.\n- Deferred: PostgreSQL-only `DISTINCT ON (cols)` (separate, gated builder).\n\n- Builder: `.distinct()` on `SourceQuery` -\u003e a `Distinct\u003cBase\u003e` wrapper (mirror\n  `Limited`/`Offset` in query.rs). `SelectAst` passthrough: same `Exprs`,\n  `Params`, `OrderClass`, `Grouped`; carries a flag.\n- Rendering: add `set_distinct()` to the `SelectSink` trait; emit `DISTINCT `\n  immediately after `SELECT`, before the first projected column, in BOTH\n  `SelectRenderSink` (render.rs) and `TestSelectSink` (squealy-test/src/sql.rs).\n  The sinks write `\"SELECT \"` in `new()`; track a `distinct` flag and emit it at\n  the first `push_projection_separator`.\n- Type-level: trivial passthrough; no new validity rules.\n\ncrates/squealy/src/query.rs, render.rs, squealy-test/src/sql.rs, lib.rs (export),\n+ tests.\n\nSQL render (test backend), per-backend `$n`/`?` unaffected, an `#[ignore]`d\nround-trip; confirm DISTINCT composes with where/group_by/order_by/limit.\n\nSmall (~half a session).\n\n`conn.from::\u003cT\u003e().distinct().select(...)` renders `SELECT DISTINCT ...`; existing\ntests green; fmt/clippy/`cargo test --workspace` pass.","files":null}]}