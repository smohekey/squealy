{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":3,"timestamp":1783816194,"nonce":"zjAGAT3V/7kGfc2b5Z1ix49ATtk=","message":"Done — shipped as two PRs.\n\n**PR A #100** (squash `d1df16b`): `ArithmeticOp::Modulo` (`%`) end-to-end — new IR variant + all three arithmetic renderers + `lower_binary` + macro emitter + KDL ser/de. A `%` check re-plans to empty structurally. Documented the SQLite integer-coercion cross-dialect caveat (`9.5 % 2` = 1 on SQLite vs 1.5 on PG/MySQL — the bug's \"no dialect divergence\" premise was wrong; maintainer-confirmed to accept + document).\n\n**PR B #101** (squash `dbdbf03`): invert a general `CAST(x AS ty)` structurally (`lower_cast` → `general_cast`, reusing `invert_pin_type`) and **delete `canonical.rs`** (~1179 lines); PG `canonical_check_expression` collapsed to the MySQL/SQLite re-parse shape. No cast-type fold is needed for the H1 macro-authored path (macro stores `Raw`, both sides re-parse to the same rep). For structural casts (KDL / hand-built / view bodies) a new `map_cast_types` walker + a `SchemaIntrospect::canonical_cast_type` seam fold each general cast's target to the backend's `canonical_*_pin_type` on both sides; `normalize_expr` and `fold_like_case_insensitivity` gained `Cast` arms.\n\n`general_cast` guards (kept `Raw`, never structured, to stay churn/loss-free on the source dialect): `I128`/`U128` (bare `numeric`/`DECIMAL(65,0)` — a 128-bit-int render target, wrong category for an authored decimal cast) and `Decimal` on MySQL/SQLite (those render bare `DECIMAL`/`NUMERIC`, dropping precision).\n\nAccepted, documented residuals (never corruption; maintainer-confirmed): a converting literal cast synthesized on a bare literal against a float column (`(1.5)::double precision` vs authored `1.5`) churns (float literals aren't guaranteed value-preserving through a cast); `IS TRUE`/`IS FALSE`, exotic-type casts (`::inet`, enums), and a general function with a direct literal argument stay `Raw`; and a `Decimal`/`I128` general cast in a package deployed **cross-dialect** to a lossier backend is an H2 residual filed as git-bug `8fe1530`.\n\nLocal codex: PR A clean after 2 rounds, PR B clean after 7 (lossy Decimal casts; the `I128` result-pin representative; `normalize`/`fold_like`/`map_cast_types` cast traversal; view-body cast fold; the H2 residual deferred + filed). CI required checks (`test` + `MSRV`) green on both PRs — PR B's first CI run caught a live-PostgreSQL round-trip regression (a stale hardcoded `Raw(\"lower((slug)::text)\")` expected value that local `cargo test` skipped because the test is `#[ignore]`); fixed and verified against live PG 17.","files":null}]}