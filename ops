{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":3,"timestamp":1783645502,"nonce":"HZomn9xi1T1cmoNmy5S9oGGZZ28=","message":"Phase 2 (2.1) — PostgreSQL view-body introspection — MERGED as PR #96 (squash `1335702`).\n\n`pg_get_viewdef(oid, true)` → `read_view_query` reconstructs a PG view's structural `ViewBody`, so a published PG view re-plans to empty (the diff's structural bar) instead of re-applying `CREATE OR REPLACE VIEW` every run. An un-lowerable body falls back to the body-unknown sentinel (with its `pg_rewrite` deps) — graceful, no regression. Subsumes 6a3940a's churn half for PG.\n\nFive reconciliations make a reconstructed body compare equal to the crate model (all applied on BOTH sides / verified against real PG):\n\n1. Result-pin canonicalization — new default-identity `SchemaIntrospect::canonical_view_body` seam applied in `canonicalize_model`; PG folds many-to-one result-pin cast spellings to one representative (`smallint←I8/I16/U8`, `integer←I32/U16`, `bigint←I64/Isize/U32/Usize`, `numeric←I128/U64/U128`, `bytea←FixedBytes/Bytes`) via a new `ViewBody::map_result_pins` walk.\n2. Single-source column resolution (reverse parser) — `pg_get_viewdef` dequalifies an unambiguous single-source column, so the reader re-binds each bare column to the scope's sole source alias; a *computed* projection-alias reference in ORDER BY/GROUP BY/HAVING stays bare.\n3. Forward path — a view's top-level projection output names are set to its declared column names (the column list overrides them at render; the builder-internal names never appear).\n4. Schema qualification — introspection empties the session `search_path` around the view pass so `pg_get_viewdef` deparses fully-qualified sources (an on-`public`/on-path relation would otherwise deparse unqualified → `schema: None` churn).\n5. View-column nullability normalized (unreliable/unrendered for a view), generalizing the body-unknown branch's ignore-nullability comparison.\n\nLocal codex: 4 rounds, 3 findings fixed (GROUP BY/ORDER BY plain-column-alias qualification; search_path qualification; FixedBytes pin fold), then clean. Live `#[ignore]` tests (`squealy-postgresql/tests/publish.rs`) prove replan-empty over a view-on-view + grouped/pinned view (off-path schema) and an on-`public` view. No IR/KDL/macro/render-format change.\n\nNEXT in the epic: 2.2 MySQL (`information_schema.VIEWS.VIEW_DEFINITION` → `read_view_query`) and 2.3 SQLite (`sqlite_master.sql` → `read_create_view`), each with its own backend `canonical_view_body`. The `1e67ff8` fallible-runtime-render split remains open.","files":null}]}