{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":3,"timestamp":1783547206,"nonce":"3SsDtjE4n52Er6ehzRRB0pwIQUo=","message":"Phase 2, PR 2.0b MERGED (PR #93, squash `b0072f3`) — second IR widening: set operations in a view body.\n\nThe view-body IR is now a recursive `ViewBody { Select(Box\u003cViewQueryModel\u003e) | Set { op, all, left, right, order_by, limit, offset } }` (+ `ViewSetOp { Union | Intersect | Except }`), so a view can be a UNION/INTERSECT/EXCEPT (±ALL) over two nested bodies. `ViewModel.query` + `SourceItem::Derived.query` flipped to `ViewBody` (a derived table can itself be a set op). Reconstructed on both paths across all 3 dialects, honoring the `set_operand_style()` seam (PG/MySQL `(…)`, SQLite `SELECT * FROM (…)`), matching the runtime set renderer.\n\nKey pieces: `render_body` (both arms always alias their projections; whole-set ORDER BY validated against the leftmost arm's outputs; `INTERSECT ALL`/`EXCEPT ALL` rejected via new `Dialect::supports_intersect_except_all()`, SQLite false); `lower_body`/`lower_set_operand`/`lower_set_op` (parenthesized/bare/SQLite-wrapper-peel/nested operands; column list names only the compound output, not the arms; outer parenthesized-body tail folded via `apply_query_tail`; unsupported outer clauses rejected before the SQLite peel); KDL `set` node ser/de; diff sentinel `query.is_empty()`.\n\nReview: local codex 5 rounds (4 findings) + GitHub @codex 3 findings — all set-op ORDER BY/operand reconstruction edges, all fixed with regression tests (roundtrip `view/set-op` + `view/set-op-arm-clause`, lower unit tests, KDL nested-set round-trip, SQLite render-rejection tests). CI green.\n\nFoundation series remaining: PR 2.0c (CTEs / WITH / recursive CTEs), then per-backend introspection wiring 2.1 PG / 2.2 MySQL / 2.3 SQLite.","files":null}]}