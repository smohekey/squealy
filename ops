{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":6,"timestamp":1782000077,"nonce":"mWnWMhHywGFYFlJOizaVqjPzFLQ=","target":"dcfdc10344fdd472fe52afae82f6aea0d111c2497f2f68c698883778ae1f64e5","message":"**Summary**\nAdd set operations: `UNION`, `UNION ALL`, `INTERSECT`, `EXCEPT`.\n\n**Plan**\n- Builder: `.union(other)`, `.union_all(other)`, `.intersect(other)`,\n  `.except(other)` on a finished select -\u003e a new top-level `SetSelect\u003cL, R\u003e`\n  query object.\n- Type-level: require both operands' `Shape::Row` to be the SAME type\n  (column-compatible). Combine params left-then-right. The result is itself\n  orderable/limitable (trailing `ORDER BY`/`LIMIT` apply to the whole set).\n- Rendering: `(\u003cleft\u003e) UNION [ALL] (\u003cright\u003e)`, reusing the borrowed-`Renderer`\n  param-sharing trick from subqueries so placeholder numbering stays continuous.\n- Structural note: `SetSelect` is a SIBLING to the select query object (not a\n  `SourceAst` chain), so it needs its own `ExecutableSelectQuery` /\n  `PreparableSelectQuery` plumbing PER BACKEND — the bulk of the work.\n\n**Files**\ncrates/squealy/src/query.rs (SetSelect + builder + executable/preparable shape),\nrender.rs + squealy-test/src/sql.rs, both backends' query.rs (execute/prepare for\nSetSelect), lib.rs, + tests.\n\n**Tests**\nRender (UNION/ALL/INTERSECT/EXCEPT, trailing ORDER BY/LIMIT), bind ordering across\nboth arms, row-type-compatibility compile-fail, per-backend, ignored round-trip.\n\n**Size**\nMedium-large (~one session) — new top-level query object across both backends.\n\n**Depends on / enables**\nRecursive CTEs (separate issue) build on UNION.","files":null}]}