{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":1,"timestamp":1781996727,"nonce":"glKpWKluItgTOGNx4aj7mY4PkBU=","title":"Cheap SQL follow-ons to fold in opportunistically alongside the larger query","message":"features. Each is sub-session; together ~one session.\n\n- [ ] `COUNT(DISTINCT col)` — flag on the aggregate AST; render `DISTINCT` inside\n      the call. (Pairs with the DISTINCT issue.)\n- [ ] `ORDER BY … NULLS FIRST/LAST` — extend `Order` + a `Dialect` seam (MySQL\n      lacks the syntax -\u003e emulate with an `IS NULL` sort key).\n- [ ] Window frames `ROWS/RANGE BETWEEN …` and named `WINDOW` clause — extend the\n      window AST/builder (explicitly deferred from the window-functions PR).\n- [ ] `FOR UPDATE` / `FOR SHARE` row locking — trailing-clause flag on the select\n      sink; `Dialect` seam for MySQL `LOCK IN SHARE MODE` vs `FOR SHARE`.\n- [ ] `INSERT … SELECT` and `UPDATE … FROM` / `DELETE … USING` — wire a select\n      source into the mutation builders.\n\nEach item follows the standard checklist: AST/builder -\u003e type-level -\u003e visit_* in\nboth renderers -\u003e Dialect seam if divergent -\u003e exports -\u003e tests (render,\nper-backend, compile-fail, ignored integration). Split into its own issue if any\nturns out larger than expected (window frames likely will).\n\nSmall each; bundle as time allows.","files":null}]}