{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":3,"timestamp":1781689499,"nonce":"A7YQDXjGDYWo4uYSwY3gPmrpwJk=","message":"Implemented in commit 23aca5a (branch feature/predicate-vocabulary).\n\n- like / not_like / ilike / not_ilike on text expressions (SqlText marker). Case-insensitivity via a new Dialect::write_like_operator hook: PostgreSQL emits native ILIKE; the default (MySQL ci collations) maps to plain LIKE.\n- in_ / not_in over an inline value list; empty list collapses to (1 = 0) / (1 = 1).\n- between / not_between (inclusive, first-class node).\n- is_true / is_false on non-null bool expressions, so a bool column goes straight into where_.\n\nBuilders on both Expr and ColumnRef; rendered in the shared renderer and the test backend; new types re-exported from the crate root. Tests cover the test backend (SQL shape + bind order), PostgreSQL ($n placeholders + native ILIKE), and MySQL (LIKE fallback). cargo fmt/clippy/test --workspace all clean.\n\nDeferred (not in this bug): nullable-text LIKE (Option\u003cString\u003e), IN against a subquery, and using these predicates inside DDL partial-index 'where =' clauses.","files":null}]}