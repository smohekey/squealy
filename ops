{"author":{"id":"c52cb19fb02c210e51792121b2bae318d19fa8efb308ae796946bf1f79891ad1"},"ops":[{"type":3,"timestamp":1782804675,"nonce":"c2PqraL9vNcbxpeTaGtHiKVLT+I=","message":"Verified fully implemented + tested; closing.\n\nAudited each checklist item and ran the render-test suites (no DB needed) across all three backends — all green:\n\n## Subqueries\n- Scalar subqueries in projections (scalar_subquery, ScalarSubqueryExprAst; result is ScalarNullable) and in predicates (.equals(scalar_subquery(...))). Test backend: test_scalar_subquery_in_projection / _as_comparison_operand.\n- IN (subquery) / NOT IN: .in_subquery / .not_in_subquery (InSubqueryPredicateAst). Test backend (uncorrelated, correlated, NOT IN, nullable, newtype, param-ordering) + PG placeholder-numbering test.\n- EXISTS / NOT EXISTS: exists() / not_exists() (ExistsPredicateAst). Test backend + PG.\n- Correlated subqueries: where_correlated / select_correlated with the Subqueries depth-tracked handle (q0_0/q1_0/q2_0 aliasing). Compile-fail guards for operand/newtype/row mismatches.\n\n## CTEs (WITH)\n- #[derive(CTE)] + selecting from a CTE as a source via .from::\u003cCte\u003e(). Single, multiple (deduped by TypeId), nested (transitive deps, topologically ordered), and CTE-referenced-only-through-a-subquery (hoisted to one leading WITH). Tested on test backend + PG + MySQL.\n- Recursive CTEs (the bug's explicit follow-up) are ALSO done: #[derive(RecursiveCTE)], WITH RECURSIVE, anchor UNION [ALL] recursive; tested on all three backends + arm-row-mismatch compile-fail.\n\n## Set operations\n- All six: .union / .union_all / .intersect / .intersect_all / .except / .except_all (SetOp enum, SetOperations trait, ExecutableSetSelectQuery). Nesting parenthesizes; trailing ORDER BY/LIMIT on the whole set; inner modifiers preserved; CTE arms hoisted to one leading WITH; row-type-mismatch + FOR-UPDATE-in-set-op compile-fails. Tested on test backend (all six) + PG + MySQL.\n\nTest runs: squealy-test cte(8)/recursive_cte(3)/set_ops(8)/subqueries(15), PG cte(3)/set_ops(1)/recursive_cte(3), MySQL cte(2)/set_ops(1)/recursive_cte(2) — all passing.\n\nNo functional gaps. Minor cross-backend test-coverage asymmetry (some features render-tested on the test backend + one real backend rather than all three; the renderer is shared via render.rs for PG/MySQL) — split to its own low-priority bug rather than holding this open.","files":null}]}